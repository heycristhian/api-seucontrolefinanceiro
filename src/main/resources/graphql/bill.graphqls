extend type Query {
    panelHome(userId:String!):PanelHome
}

type Panel {
    date:String,
    year:Int
    amount:Float,
    title:String
}

type PanelHome {
    panelQuantity:Int,
    panels:[Panel]
}